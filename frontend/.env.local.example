# Copy this to .env.local for local development
# When running frontend on port 3000 and backend on port 8000:
REACT_APP_API_URL=http://localhost:8000

# When using Vercel dev (vercel dev) — leave empty to use relative /api:
# REACT_APP_API_URL=
