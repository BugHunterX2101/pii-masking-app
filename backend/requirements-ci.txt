# CI test dependencies — mirrors the production detection stack (small English
# spaCy model + RapidOCR) so the document-handler tests run for real instead
# of failing or being skipped. Presidio loads its NLP engine even for
# regex-only entity lists, so the en_core_web_sm wheel is required.
pytest>=8.0,<9.0
presidio-analyzer==2.2.35
presidio-anonymizer==2.2.35
spacy==3.7.4
langdetect==1.0.9
Faker==24.4.0
dnspython>=2.6.0,<3.0.0
python-stdnum>=1.19,<2.0
# English spaCy NER model (small, ~12 MB wheel) — required by AnalyzerEngine
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.0/en_core_web_sm-3.7.0-py3-none-any.whl
# Document-handler tests build real PDF/DOCX/image fixtures
PyMuPDF==1.24.1
python-docx==1.1.0
rapidocr-onnxruntime==1.3.24
onnxruntime>=1.17.0,<2.0.0
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0
